# mediasoup-demo 数据流文档（按用户使用流程）

> 本文档是 `项目架构与数据流文档.md` 的重构版，**按一个真实用户从打开网页到离开房间的
> 完整使用流程**组织数据流，并对照以下三类运行时日志逐条核对源码后写成：
>
> - `user_add_console.log` —— 浏览器 console（一次真实会话，peerId `hhnix0qe`，roomId `dev`）
> - `app.log` —— Vite 前端 dev server 启动日志
> - `server.log` —— SFU 服务端运行日志
>
> 核对基准源码：`server/src/*`、`app/src/*`、`server/config.mjs`、
> `server/node_modules/mediasoup/node/lib/*`（mediasoup 3.24.2 已安装）。
>
> 文中所有「不确定」处均用 ⚠️ 标注，并在末尾「最不确定部分」集中说明。

---

## 0. 前置：本次会话的参与方与配置

从 `server.log` 开头可确认服务端运行配置（与 `server/config.mjs` 一致）：

| 配置项 | 值 | 来源 |
|--------|-----|------|
| domain | `localhost` | config.mjs:22 |
| 信令端口 | `4443`（HTTPS/WSS，带 TLS cert/key） | config.mjs:28 |
| numWorkers | `12`（= CPU 核数，`os.cpus().length`） | config.mjs:42，server.log `numWorkers: 12` |
| WebRtcServer | UDP + TCP，`announcedAddress 127.0.0.1`，端口 `44444` 起按 Worker 递增 | config.mjs:133-152 |
| PlainTransport | UDP 端口区间 `40000-40999` | config.mjs:180-188 |
| 音频编码 | opus 48000 双声道 | config.mjs:74-80 |
| 视频编码 | VP8 / VP9 / H264（两组 profile） | config.mjs:81-120 |
| rtcstatsUrl | `undefined`（未配置） | config.mjs:205 |

本次会话：单用户 `hhnix0qe`（displayName `Vaporeon`，Chrome 151）进入 `dev` 房间
```
protoo-client:WebSocketTransport constructor() [url:wss://localhost:4443/?roomId=dev&peerId=hhnix0qe&consumerReplicas=0&usePipeTransports=false&disableBwe=false, options:undefined] +0ms

```
`usePipeTransports=false`、`consumerReplicas=0`、`disableBwe=false`（见 console 里 protoo URL）。

> ⚠️ `server.log` 尾部实际有 **3 次会话**，本文件全部只引用**第一次 `hhnix0qe`
> （`09:36:56.698` 起，完整）**，它才与 `user_add_console.log` 对应。另外两次
> （`09:38:23.485` 的 hhnix0qe 中断会话、`09:38:24.967` 的 `cjpsxhfs` 第二次会话）不用于佐证。

---

## 1. 阶段一：用户打开网页（app 端，纯前端）

源码：`app/src/index.jsx`

1. `domready` 后调用 `run()`（index.jsx:39-45）。
2. `run()` 生成身份与参数（index.jsx:61-112）：
   - `peerId = randomString({ length: 8 }).toLowerCase()` → 本次为 `hhnix0qe`；
   - `roomId = urlParser.query.roomId`，**URL 未带则随机生成并 pushState 回写 URL**（index.jsx:128-133）；
   - `displayName` 优先取 URL / cookie，否则 `randomName()`（index.jsx:184-194）。
     console 里 `displayName:"Vaporeon"` 来自 `randomName.js`（用 `pokemon` 包随机取宝可梦名）。
   - 其余 40+ 查询参数在此解析（forceTcp / forceH264 / simulcast 层数 / e2eKey 等）。
3. `new RoomClient({...})`（index.jsx:207-242），并把实例挂到 `window.CLIENT`。
4. React 渲染 `<Room />`（index.jsx:250-259）。

日志对应（console）：

```
mediasoup-demo DOM ready
mediasoup-demo run() [environment:development]
mediasoup-demo:RoomClient constructor() [roomId:"dev", peerId:"hhnix0qe", displayName:"Vaporeon", device:chrome]
```

---

## 2. 阶段二：建立信令 WebSocket 连接（protoo，端口 4443）

源码：`app/src/RoomClient.js`、`app/src/urlFactory.js`、`app/src/components/Room.jsx`

1. `<Room />` 的 `componentDidMount()` 调用 `roomClient.join()`（Room.jsx:157-160）。
2. `join()` 构造 protoo 连接（RoomClient.js:342-355）：

   ```js
   const protooTransport = new protooClient.WebSocketTransport(this._protooUrl);
   this._protoo = new protooClient.Peer(protooTransport);
   this._protoo.on('open', () => this._joinRoom());   // 连接成功才进 _joinRoom
   ```

3. protoo URL 由 `getProtooUrl()` 生成（RoomClient.js:238-244 → urlFactory.js:12-16）：

   ```
   wss://localhost:4443/?roomId=dev&peerId=hhnix0qe&consumerReplicas=0&usePipeTransports=false&disableBwe=false
   ```

   ⚠️ `urlFactory.js` 并非「固定 4443」：当 `hostname === 'test.mediasoup.org'` 时端口改 `4444`
   （urlFactory.js:5-7）。本机 localhost 才是 4443。

日志对应（console）：

```
protoo-client:WebSocketTransport constructor() [url:wss://localhost:4443/?roomId=dev&peerId=hhnix0qe&consumerReplicas=0&usePipeTransports=false&disableBwe=false, ...]
protoo-client:Peer emit "open"
mediasoup-demo:RoomClient _joinRoom() +455ms
```

---

## 3. 阶段三：服务端接受连接并创建 Room/Peer

源码：`server/src/WsServer.ts`、`server/src/Server.ts`、`server/src/Room.ts`、`server/src/Peer.ts`

1. `WsServer.handleProtooServer()` 的 `connectionrequest` 回调（WsServer.ts:79-148）：
   - 校验 Origin 是否等于 `https://localhost:4443`（WsServer.ts:81-90，`computeHttpOriginHeader` 由 `domain` 算出）；
   - 解析查询参数 `roomId/peerId/consumerReplicas/usePipeTransports/disableBwe`（WsServer.ts:98-105）；
   - `emit('get-or-create-room', ...)` 触发房间创建（WsServer.ts:123-130）。
2. `Server.getOrCreateRoom()`（Server.ts:323-413）：按 `roomId` 懒创建 Room，`AwaitQueue` 串行化防并发。
   - 取一个 Worker 建 `producerRouter`（Server.ts:370-385）；
   - `usePipeTransports=false` 时 **不另建 consumerRouter**，两者指向同一 Router（Server.ts:387-391，⚠️ 见末尾不确定点 #3）；
   - `Room.create()` 内部再创建 `audioLevelObserver`、`activeSpeakerObserver`、`protoo.Room`、`Bot`（Room.ts:140-155）。
3. `Room.processWsConnection()`（Room.ts:288-313）：`protooRoom.createPeer` + `Peer.create`，先放入 `#joiningPeers`。
4. **`Peer` 构造函数主动下发 `mediasoupVersion` 通知**（Peer.ts:218-219）：

   ```ts
   this.notify('mediasoupVersion', { version: mediasoup.version });
   ```

   这是「服务端→客户端」的第一条消息，原文档信令流程遗漏了这一条。

日志对应（server.log 第一次 hhnix0qe 会话，行 1684-1694）：

```
09:36:56.698 WsServer protoo WebSocket connection request [roomId:'dev', peerId:'hhnix0qe', address:'127.0.0.1', origin:'https://localhost:5555']
09:36:56.699 Server getOrCreateRoom() | creating a new Room [roomId:'dev', usePipeTransports:false]
09:36:56.700 Room create() ...
09:36:56.700 Bot create()
09:36:56.703 Room processWsConnection() [peerId:'hhnix0qe']
09:36:56.703 Peer create() / constructor()
09:36:56.704 Peer:[peerId:hhnix0qe] ··> notification [name:'mediasoupVersion']
```

> 注意：`origin` 校验的是 `https://localhost:5555`（前端 Vite 页面的 Origin），
> 与信令服务自身算出的 `https://localhost:4443`（Server.ts:122-134 `computeHttpOriginHeader`）
> 做比较，由 `utils.areSameHttpOrigins` 判断——它**只比较 scheme + hostname，忽略端口**
> （utils.ts:22-41），因此 `5555` vs `4443` 仍视为同源，校验通过。

---

## 4. 阶段四：加入房间（`_joinRoom()` 完整顺序）

源码：`app/src/RoomClient.js:2337-2640`。**这是原文档信令流程遗漏最多的一环**，按代码实际顺序：

| 步骤 | 动作 | 代码位置 | 日志佐证 |
|------|------|----------|----------|
| 4.1 | `mediasoupClient.Device.factory()` 探测浏览器 handler（Chrome151→Chrome111 handler） | 2343-2345 | `Device factory() ... using Chrome111 handler` |
| 4.2 | `getRtcStatsUrl` 请求（若 URL 未带 `rtcstatsUrl` 参数） | 2358 | `request [method:getRtcStatsUrl]` |
| 4.3 | `getRouterRtpCapabilities` 请求 | 2368-2370 | `request [method:getRouterRtpCapabilities]` |
| 4.4 | `device.load({ routerRtpCapabilities })` | 2372-2375 | `Device load() ... succeeded` |
| 4.5 | 获取麦克风权限 hack（关闭音轨，120s 后停，为绕过自动播放策略） | 2381-2390 | —（无独立日志） |
| 4.6 | `createWebRtcTransport`（producer）→ `createSendTransport()` | 2394-2429 | `createSendTransport()` |
| 4.7 | `createWebRtcTransport`（consumer）→ `createRecvTransport()` | 2503-2537 | `createRecvTransport()` |
| 4.8 | `join` 请求（携带 `displayName/device/rtpCapabilities`） | 2559-2565 | `request [method:join]` |
| 4.9 | 成功 → `setRoomState('connected')` + 提示 "You are in the room!" | 2567-2577 | — |
| 4.10 | 把 join 响应里的已有 peers 加入 Redux | 2579-2590 | — |
| 4.11 | `enableMic()` / `enableWebcam()` / `enableChatDataProducer()` / `enableBotDataProducer()` | 2602-2620 | `enableMic() ... enableWebcam() ...` |

服务端对应处理（`Peer.ts handleProtooRequest`）：
- `getRtcStatsUrl` → `get-rtcstats-url` 事件 → `Room` 转发 → `Server` 读 `config.providers.rtcstatsUrl`（Peer.ts:781-787、Room.ts:526-530、Server.ts:659-661）。
- `getRouterRtpCapabilities` → `get-router-rtp-capabilities` 事件 → 返回 `consumerRouterRtpCapabilities`（Peer.ts:789-795、Room.ts:532-534）。
- `createWebRtcTransport` → `create-webrtc-transport` 事件 → `Room` 用 `producerRouter/consumerRouter` + 对应 `WebRtcServer` 建 transport（Room.ts:536-593）。
- `join` → 标记 joined，`emit('joined')`（Peer.ts:797-816）。

`Room` 收到 `joined` 后（Room.ts:468-505）：
1. `#joiningPeers` → `#peers`；
2. 回调返回房间内已有 peers 的序列化列表；
3. 通知其他 peer `newPeer`；
4. 遍历其他 peer 的 producer，让新 peer `consume()`；
5. **`void peer.consumeData({ dataProducer: this.#bot.getDataProducer() })`** —— 让新 peer 消费 Bot 的数据通道（Room.ts:504）。

日志对应（server.log 第一次 hhnix0qe 会话，行 1703-1716）：

```
09:36:57.272 <== request [name:'join'] → success response [name:'join']
09:36:57.273 Peer consumeData() [peerId:undefined, dataProducerId:'c18a8143...', channel:'bot']
09:36:57.276 ==> request [name:'newDataConsumer']      ← 服务端主动向客户端发请求
09:36:57.284 <== request [name:'connectWebRtcTransport'] ×2  ← DTLS 连接
09:36:57.301 <== request [name:'produceData']  (chat)
09:36:57.310 <== request [name:'produceData']  (bot) → Bot consumeData()
```

> 关键点：protoo 是**双向**的。除客户端 `request` 外，服务端也会用 `request` 主动向客户端
> 发 `newConsumer` / `newDataConsumer`（客户端在 `this._protoo.on('request', ...)` 中处理，
> RoomClient.js:386）。

---

## 5. 阶段五：开麦 / 开摄像头（produce 信令）

源码：`app/src/RoomClient.js`

### 5.1 开麦 `enableMic()`（964-1070）

1. `navigator.mediaDevices.getUserMedia({ audio: true })`（983-987）；
2. `this._sendTransport.produce({ track, codecOptions: {opusStereo/opusDtx/opusFec/opusNack}, appData: { source: 'audio' } })`（1019-1027）；
3. mediasoup-client 内部触发 transport 的 `produce` 事件 → 回调里 `this._protoo.request('produce', { transportId, kind, rtpParameters, appData })`（2448-2465）；
4. 服务端 `case 'produce'`：`transport.produce(...)` 建 Producer，`emit('new-producer')`，`accept({ producerId })`（Peer.ts:869-894）。

### 5.2 开摄像头 `enableWebcam()`（1112-1322）

1. 先 `_updateWebcams()` 枚举设备（`enumerateDevices`，2642-2666）；
2. `getUserMedia({ video: {...} })` 取摄像头 track；
3. `this._sendTransport.produce({ track, ..., appData: { source: 'video' } })`（1266-1275），流程同麦克风。

日志对应（console）：

```
mediasoup-demo:RoomClient enableMic() +526ms
mediasoup-demo:RoomClient enableWebcam() +1ms
mediasoup-client:Transport produce() [track:MediaStreamTrack]        ← 音频
protoo-client:Peer request() [method:produce, id:8179090]
mediasoup-client:Transport produce() [track:MediaStreamTrack]        ← 视频
protoo-client:Peer request() [method:produce, id:2479393]
```

server.log 对应（第一次 hhnix0qe 会话，行 1720-1726）：

```
09:36:57.638 <== request [name:'produce'] → success   ← 音频 (enableMic)
09:36:57.658 ··> notification [name:'producerScore']
09:36:57.905 ··> notification [name:'activeSpeaker']
09:36:58.639 <== request [name:'produce'] → success   ← 视频 (enableWebcam)
```

---

## 6. 阶段六：媒体 RTP 实际传输路径与转发机制（关键）

### 6.1 两条独立的链路：信令 vs 媒体

媒体数据**不经过 Node.js 主进程**。整个系统有两条物理上分开的链路：

| 链路 | 端口 | 协议 | 谁处理 | 内容 |
|------|------|------|--------|------|
| 信令 | 4443 | WSS/HTTPS | Node.js（WsServer→Server→Room→Peer） | protoo 请求/通知，控制 Producer/Consumer 的建与拆 |
| 媒体 | 44444+（UDP/TCP） | SRTP/RTCP | C++ `mediasoup-worker` 子进程 | 真正的 RTP/RTCP 报文 |

所以「在 Node 代码里找发送 RTP 的地方」是找不到的——**它根本不在 Node 里**。Node 只做
控制面（编排 Producer/Consumer），媒体面由 worker 完成。

### 6.2 端到端转发路径

```
浏览器A (发送)                        mediasoup-worker (C++ 子进程)                    浏览器B (接收)
  sendTransport ──SRTP/UDP:44444──▶ WebRtcTransport(producer方向) ─▶ Router ─▶ Producer
                                                                        │ 自动转发
  recvTransport ◀──SRTP/UDP:44444── WebRtcTransport(consumer方向) ◀─ Consumer ◀─┘
```

- WebRtcServer 端口由 `webRtcServerOptions.listenInfos` 决定（config.mjs:133-152，44444 起，
  每 Worker 递增，Server.ts:166 `port += idx`）。
- 每次 `createWebRtcTransport` 都挂在对应 Worker 的 WebRtcServer 上（Room.ts:538-595，
  `webRtcServer: mediasoupWebRtcServer` 在 Room.ts:572）。

### 6.3 为什么能「自动转发」：转发发生在 C++ Router 内部，Node 只做编排

mediasoup 的 `Router` 是一个 C++ 实现的 RTP 转发器。「自动」的含义是：**Node 代码从不搬运
任何 RTP 包**，它只做两件编排动作：

1. 发送方 `produce` 时，把 RTP 流注册成一个 `Producer`（挂在 Router 上）；
2. 接收方 `consume` 时，在同一个 Router 上建一个 `Consumer`，用 `producerId` 指向那个 Producer。

一旦 `Consumer` 建立，Router 在 C++ 层就会把 Producer 收到的 RTP 自动复制/转发给所有指向它的
Consumer，Node 不再介入。这就是「自动转发」的实质。

`Consumer` 的建立有两个触发时机，最终都落到 `Peer.consume()`：

- **新 peer 加入**：`Room` 收到 `joined`（Room.ts:470），遍历已有 peer 的 producer 让新 peer
  consume（Room.ts:482-504，`peer.consume(...)` 在 Room.ts:486）；
- **已有 peer 新开 producer**：`Room` 收到 `new-producer`（Room.ts:598-624），遍历其他 peer
  调 `otherPeer.consume({ producer })`（Room.ts:609）。

`Peer.consume()`（Peer.ts:286-402）的核心是
`transport.consume({ producerId, rtpCapabilities, enableRtx:true, paused:true, ignoreDtx:true })`
（Peer.ts:340-351）——这一步就是让 C++ Router 把 Producer 和 Consumer 关联起来。

### 6.4 RTP/RTCP 在代码哪里「发送」：只有控制面可定位

**控制面（本仓库可定位的代码）：**

- 发送方注册 Producer（客户端）：
  - `app/src/RoomClient.js:1019`（开麦）、`:1266`（开摄像头）→ `this._sendTransport.produce({ track, ... })`；
  - transport 的 `produce` 事件回调 `RoomClient.js:2448-2465` → `this._protoo.request('produce', ...)`（`:2453`）；
  - 服务端 `server/src/Peer.ts:884-909` `case 'produce'` → `transport.produce(...)`（`:890`）创建 Producer，
    `emit('new-producer')`（`:904`）。
- 接收方创建 Consumer（服务端）：见 6.3 的 `Peer.consume()`（Peer.ts:340-351）。

**媒体面（真正的 RTP/RTCP 收发，不在本仓库）：**

- 在 C++ 二进制 `server/node_modules/mediasoup/worker/out/Release/mediasoup-worker` 内完成。
- Node 与 worker 之间只通过 stdio 管道 fd3/fd4 传 FlatBuffers 控制消息（`node/lib/Channel.js:104-120`），
  消息类型只有 Request/Response/Notification，**不含任何 RTP 内容**。
- RTCP 同理：NACK/PLI/TWCC/RR 都在 worker 内部处理，走的是和 RTP 同一条 SRTP 通道（rtcpMux），
  Node 代码里搜不到任何 RTCP 收发。

### 6.5 转发后对端从哪里接收

接收端浏览器在客户端代码里消费：

- `app/src/RoomClient.js:395` `case 'newConsumer'` → `this._recvTransport.consume({ id, producerId, kind, rtpParameters, ... })`（`:419`）；
- 拿到 `consumer.track`（`:462`）存入 Redux，最终 `app/src/components/PeerView.jsx:532` 的
  `_setTracks` 把 track 挂到 `audioElem.srcObject`（`:554`）/ `videoElem.srcObject`（`:569`）播放。

即对端不是从某个「缓冲区」取，而是浏览器 B 的 `recvTransport` 对应的 `RTCRtpReceiver` 直接
收到 worker 通过 SRTP 推来的 RTP，解码成音视频 track 播放。

> ⚠️ 本仓库日志只能证明**信令**流程；RTP 报文本身在 C++ worker 内完成，无法从这三份日志
> 直接看到（见末尾不确定点 #2，「转发在 C++ 内部」是基于「Node 侧无任何 RTP 处理 + mediasoup
> 设计」的推断，未直读 worker C++ 源码）。

---

## 7. 阶段七：数据通道（聊天 / Bot）

源码：`app/src/RoomClient.js`、`server/src/Peer.ts`、`server/src/Room.ts`、`server/src/Bot.ts`

1. `enableChatDataProducer()`（1876）→ `sendTransport.produceData({ appData: { channel: 'chat' } })`；
2. `enableBotDataProducer()`（1967）→ `produceData({ appData: { channel: 'bot' } })`；
3. 两者都走 transport 的 `producedata` 事件 → `this._protoo.request('produceData', ...)`（2468-2498）；
4. 服务端 `case 'produceData'`：`transport.produceData(...)` → `emit('new-data-producer')`（Peer.ts:896-921）；
5. `Room` 按 channel 分发（Room.ts:625-654）：
   - `chat` → 转发给其他 peer `consumeData()`；
   - `bot` → `Bot.consumeData()`（Bot 消费聊天消息并自动回复）。

反向（Bot → 用户）：Room 在 `joined` 时让 peer `consumeData(bot)`（阶段四），随后服务端
`newDataConsumer` 请求下发，客户端 `recvTransport.consumeData()` 消费（RoomClient.js 里
`case 'newDataConsumer'`）。

连接建立后：`sctp-connected` 事件触发服务端给用户发欢迎消息（Room.ts:515-524）：

```ts
void peer.sendMessage(`Welcome ${peer.displayName}! ☺️`);
```

日志对应（console）：

```
mediasoup-client:Transport produceData() ...
protoo-client:Peer request() [method:produceData, id:492979]  ← chat
protoo-client:Peer request() [method:produceData, id:3246504] ← bot
mediasoup-demo:RoomClient DataConsumer "message" event [streamId:0, length:24]  ← 欢迎消息
```

---

## 8. 阶段八：其他 peer 消费（SFU 转发核心）

源码：`server/src/Room.ts:596-622`、`server/src/Peer.ts:271-387`、`app/src/RoomClient.js:395-466`

1. `Room` 收到 `new-producer`（Room.ts:596-622）：
   - 若 `usePipeTransports` 则 `pipeToRouter` 到 consumerRouter（本会话为 false，跳过）；
   - 遍历 `getOtherPeers(peer)`，对每个其他 peer `consume({ producer })`；
   - 若为音频 producer，额外 `audioLevelObserver.addProducer` + `activeSpeakerObserver.addProducer`（Room.ts:613-621）。
2. `Peer.consume()`（Peer.ts:271-387）：
   - `get-can-consume` 检查 `router.canConsume`；
   - `transport.consume({ paused: true, enableRtx: true, ... })` 建 Consumer；
   - `this.request('newConsumer', {...})` 下发（Peer.ts:352-363）；
   - 客户端响应后再 `consumer.resume()`。
3. 客户端 `case 'newConsumer'`（RoomClient.js:395-466）：`recvTransport.consume(...)`，
   把 consumer 存进 `_consumers` 并 dispatch 到 Redux；最终 `PeerView.jsx` 把 `consumer.track`
   挂到 `<video>/<audio>.srcObject` 播放（PeerView.jsx `_setTracks`，方法本体在 532 行起，
   原文档写 565-592 略有偏差）。

> 本会话只有一名用户（无他人），因此日志中**没有出现 `newProducer`→`consume` 链路**。
> 这一阶段的代码路径是通过源码确认的，而非本次日志实测。

### 8.1 旁路降噪（in-line processing，可选改造）

> 本项目新增了旁路降噪能力。当 `server/config.mjs` 里 `processing.enabled = true` 时，
> 除了上述「转发给其他 peer」外，还会额外执行旁路逻辑。

在 `Room.ts` 的 `new-producer` 回调里，转发给其他 peer 之后，追加了一段（Room.ts:634-640）：

```ts
// Bypass a copy of the audio stream to the processing service.
if (this.#mediaRelay && producer.kind === 'audio') {
    void this.#mediaRelay.bypass(producer);
}
```

`MediaRelay.bypass()`（`server/src/MediaRelay.ts`）做的事：

1. 从 `producer.consumableRtpParameters.codecs` 里找到 opus codec，动态构造
   `rtpCapabilities`（channels/clockRate/payloadType 与原 producer 对齐）；
2. 在出方向 PlainTransport 上 `consume({ producerId, ignoreDtx:false, enableRtx:false })`，
   把 audio 流复制一份推给降噪服务（`remoteIp:remotePort`）。

回传方向：降噪服务以 **BroadcasterPeer "denoiser"** 身份，走现有 HTTP API
（`createBroadcasterPeer → join → createPlainTransport → connect → produce`）回传降噪流，
复用 `BroadcasterPeer` 机制自动转发给观察端，形成独立 peer 条目，供 A/B 对比。

> 防回环：回传流走 `broadcasterPeer.on('new-producer')` 分支，而旁路入口只在
> `peer.on('new-producer')`，两条路径天然隔离，无需额外标记。
> 详见 `server/docs/降噪旁路项目开发文档.md`。

---

## 9. 阶段九：关摄像头 / 静音 / 离开

源码：`app/src/RoomClient.js`、`server/src/Peer.ts`、`server/src/Room.ts`

- **关摄像头** `disableWebcam()`：`producer.close()` + `this._protoo.notify('closeProducer', { producerId })`；
  服务端 `case 'closeProducer'` → `producer.close()`（Peer.ts:681-688）。
- **静音** `muteMic()`：`producer.pause()` + `notify('pauseProducer', ...)`；
  服务端 `case 'pauseProducer'` → `producer.pause()`（Peer.ts:690-697）。
- **离开**：protoo 断开 → `Peer` 的 `close` 事件 → `Room` 从 `#peers` 移除 → `mayClose()`，
  若房间内无 peer 则 `close()` 关闭 Room（Room.ts:371-389、460-466）。

日志对应（console + server.log 本次会话）：

```
RoomClient.js:1325 disableWebcam() → closeProducer (视频)
RoomClient.js:1089 muteMic()       → pauseProducer (音频)
server.log:
09:37:13.715 <·· notification [name:'closeProducer']
09:37:14.274 <·· notification [name:'pauseProducer']
...
09:38:23.397 Peer:[peerId:hhnix0qe] close()
09:38:23.400 Room:[roomId:dev] last Peer in the Room left, closing the Room
```

---

## 10. 信令时序总览（修正版）

```
浏览器 (app)                           SFU 服务端 (server)
   │  WebSocket wss://localhost:4443/?roomId&peerId&consumerReplicas&usePipeTransports&disableBwe
   │──────────────────────────────────────────▶ WsServer 校验 Origin、解析参数
   │                                            Server.getOrCreateRoom → Room.create(Bot)
   │                                            Room.processWsConnection → Peer.create
   │  ◀──────── notification: mediasoupVersion ─┤  (Peer 构造时下发)
   │  request: getRtcStatsUrl ─────────────────▶│
   │  ◀──────── response ───────────────────────┤
   │  request: getRouterRtpCapabilities ───────▶│
   │  ◀──────── response (rtpCapabilities) ─────┤
   │  [device.load()]                           │
   │  request: createWebRtcTransport(producer)─▶│
   │  ◀──────── response (transport params) ────┤
   │  [createSendTransport()]                   │
   │  request: createWebRtcTransport(consumer)─▶│
   │  ◀──────── response (transport params) ────┤
   │  [createRecvTransport()]                   │
   │  request: join {displayName,device,rtpCap}▶│  Peer.join → Room.joined
   │  ◀──────── response: {peers:[...]} ────────┤   (并 consumeData(bot))
   │  ◀──────── request: newDataConsumer ───────┤   (服务端主动请求)
   │  request: connectWebRtcTransport ×2 ──────▶│   (DTLS 建连)
   │  request: produceData(chat/bot) ──────────▶│
   │  request: produce(audio) ─────────────────▶│   → new-producer
   │  request: produce(video) ─────────────────▶│   → new-producer
   │  ◀──────── notification: producerScore/activeSpeaker/speakingPeers
   │  notify: closeProducer / pauseProducer ───▶│   (关摄像头/静音)
```

---

## 11. 原文档偏差修正对照表

| # | 原文档表述 | 核对结果 |
|---|-----------|----------|
| 1 | 6.2 节 worker 路径 `worker/out/Release/mediasoup-worker.exe` | ❌ 本机（macOS）为 `mediasoup-worker`，**无 `.exe`**（`server/node_modules/mediasoup/worker/out/Release/mediasoup-worker`） |
| 2 | 信令流程起点是 `getRouterRtpCapabilities` | ❌ 遗漏了更早的 `getRtcStatsUrl` 请求、以及服务端下发的 `mediasoupVersion` 通知 |
| 3 | 5.3 节「每个 Room 创建 producerRouter 和 consumerRouter」 | ⚠️ 误导：`usePipeTransports=false` 时两者是**同一个 Router**（Server.ts:387-391），只有 `=true` 才分属两个 Worker |
| 4 | 5.4 节「urlFactory.js 固定 wss://…:4443」 | ⚠️ 有 `test.mediasoup.org` 特例（改 4444），本机 localhost 才是 4443 |
| 5 | 8 阶段一「构造 protoo URL（行 238-244）… new protooClient.Peer」 | ⚠️ `getProtooUrl` 在 238-244，但 `new protooClient.Peer(...)` 实际在 `join()` 的 347-355，不在构造函数 |
| 6 | 阶段四 `_joinRoom` 描述缺 autoplay hack | ⚠️ 漏了 2381-2390 的麦克风权限 hack |
| 7 | 7 阶段六 PeerView `_setTracks` 行 565-592 | ⚠️ 方法本体在 532 行，`srcObject` 赋值在 554/569/591 |
| 8 | 8 阶段一 peerId 行 61-63 | ⚠️ peerId 实际在 62 行、roomId 在 63 行；且未提「URL 无 roomId 时随机生成」逻辑 |
| 9 | 未覆盖 displayName 随机（"Vaporeon"）来源 | ❌ 来自 `randomName.js` + `pokemon` 包 |
| 10 | 6.4 节「MESSAGE_MAX_LEN = 8MB」 | ⚠️ 实际 `Channel.js` 为 `8388608` = **8 MiB**（且是 8 MiB 而非 8 MB 的措辞差异） |
| 11 | 6.6 节 `this.#nextId++` | ⚠️ 实际为 `++this.#nextId`（且 4294967295 回绕，Channel.js:222-227） |
| 12 | 6.4 节「3.24.x 已把旧 JSON 替换为 FlatBuffers」 | ⚠️ FlatBuffers 在 **v3 整条线即采用**，非 3.24.x 才切换；建议改为「v3 起」 |
| 13 | 未覆盖 producerScore / speakingPeers / activeSpeaker / ICE 断开 | ❌ 这些是运行期高频通知，日志大量出现，但两份文档都未讲清机制（见下节复核结论） |

> 除上述偏差外，原文档的**依赖版本、端口、核心行号**（Room.ts / Peer.ts / Server.ts /
> WsServer.ts 的 join/produce/consume/getOrCreateRoom/processWsConnection/connectionrequest
> 等行号）与源码逐条吻合；第六节「C++ ↔ TS 交互」中 `node/lib/*.js` 与 `worker/fbs/*.fbs`
> 部分（fd 3/4、FlatBuffers、`finishSizePrefixed`）也已在本仓库的 `node_modules/mediasoup`
> 内验证通过。

---

## 12. 交叉评审结论（主检查 × 独立 agent 复核）

主检查（本文档编写者）与独立复核 agent 各自独立核对源码/日志后，交叉比对结果如下：

### ✅ 已确证（原文档正确，无需修改）

1. **C++ 侧 fd 编号**（原文档 6.3 节）：确证。mediasoup v3 的 `worker/src/main.cpp`
   定义 `ConsumerChannelFd{3}`、`ProducerChannelFd{4}`。命名视角相反但方向一致：
   - fd3 = Node `producerSocket` 写端 = C++ `ConsumerSocket` 读端（从 Node 收消息）；
   - fd4 = C++ `ProducerSocket` 写端 = Node `consumerSocket` 读端（向 Node 发消息）。
   并非矛盾。
2. **原文档 6.6/6.7 引用的 C++ 文件**（`Worker.cpp:238-364` HandleRequest、`ChannelSocket.cpp`
   帧解析、`ChannelRequest.cpp` Accept/Error/SendResponse、`ChannelNotifier.cpp:16-28`）：
   确证存在，行号与 v3 分支吻合。
3. **「媒体 RTP 不经过 Node 主进程」**：确证。`WebRtcServer` 是 worker 内组件，多 transport
   共享监听端口，网络流量由 C++ worker 处理；Node 只做控制面（`Worker.js` 发
   `WORKER_CREATE_WEBRTCSERVER` 请求，`Room.ts:564-575` 把 transport 挂到 worker 的
   WebRtcServer 上）。
4. **`usePipeTransports=false` 时单 Router**（本会话）：确证，Server.ts:387-391。

### ✅ 复核发现并已修正的错误

1. **日志归属错乱（最重要）**：`server.log` 尾部有 3 次会话，本文初稿把第二次 `hhnix0qe`
   （中断会话）和 `cjpsxhfs` 的 join/produce 误安到了 console 对应的第一次 `hhnix0qe` 头上。
   已全部改为引用第一次 `hhnix0qe`（`09:36:56` 起）会话。
2. **遗漏的机制**（已补进正文/对照表）：
   - `producerScore`：`Peer.ts:1059-1060` `producer.on('score')→notify`；
   - `speakingPeers`/`activeSpeaker`：`Room.ts:906-938` 由 `audioLevelObserver`
     （'volumes'/'silence'）与 `activeSpeakerObserver`（'dominantspeaker'）驱动，约 800ms 一次；
   - ICE 断开→关闭：`Peer.ts:1012-1019` `icestatechange→close`；
   - `peerClosed` 通知：`Room.ts:507-513`。
3. **细节措辞**：`++this.#nextId`（非 `this.#nextId++`）；`MESSAGE_MAX_LEN=8388608`（8 MiB）；
   FlatBuffers 为 v3 起即采用（非 3.24.x 才切换）。

### ⚠️ 仍存的小范围不确定（不影响结论）

1. C++ 文件行号是基于 v3 分支 HEAD 核对，未逐 commit 比对到 3.24.2 精确 tag，行号可能有
   ±几行漂移（但文件结构与常量名已确认）。
2. 第二次 `hhnix0qe` 会话为何在 `getRouterRtpCapabilities` 后中断（页面刷新/用户重进？）
   无法仅凭日志判定。

### 最终答案要点

- 原文档**总体可靠**：依赖版本、端口、核心行号、C++ 交互机制均与源码/日志吻合。
- 需要修正的就 13 处（见第 11 节对照表），其中**唯一实质性事实错误**是 worker 二进制
  路径的 `.exe` 后缀（应为无后缀的 `mediasoup-worker`）；其余多为「遗漏」（getRtcStatsUrl、
  mediasoupVersion、运行期通知机制）与「易误导表述」（单/双 Router、urlFactory 端口、行号小漂移）。
- 数据流正确顺序为：`mediasoupVersion` 通知 → `getRtcStatsUrl` → `getRouterRtpCapabilities`
  → `createWebRtcTransport`(send) → `createWebRtcTransport`(recv) → `join` →（服务端
  `consumeData(bot)` + `newDataConsumer`）→ `connectWebRtcTransport`×2 → `produceData`×2 →
  `produce`(audio) → `produce`(video)，随后高频 `producerScore/activeSpeaker/speakingPeers`
  通知，关摄像头/静音对应 `closeProducer/pauseProducer`，断开触发 Room 级关闭。

---

## 13. 附：端口汇总（与日志一致）

| 端口 | 协议 | 用途 | 日志佐证 |
|------|------|------|----------|
| 5555 | HTTPS | Vite 前端 dev server | app.log `Local: https://localhost:5555/` |
| 4443 | HTTPS/WSS | protoo 信令 | console `wss://localhost:4443/...`、server.log |
| 44444+ | UDP/TCP | WebRtcServer 媒体（每 Worker 递增） | config.mjs:133-152 |
| 40000-40999 | UDP | PlainTransport（FFmpeg/GStreamer） | config.mjs:180-188 |
| 5000 | UDP | 降噪服务监听端口（旁路方向，`processing.remotePort`，可选） | config.mjs `processing` 段 |
