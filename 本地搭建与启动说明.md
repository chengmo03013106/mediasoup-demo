# mediasoup-demo 本地搭建与启动说明

> 本文档记录了在本地（Windows）搭建 mediasoup-demo 服务的完整过程，
> 包括项目理解、依赖安装、文件增删改查、以及后续的启动方式。

---

## 一、项目理解

**mediasoup-demo v3** 是一个基于 [mediasoup](https://mediasoup.org) 的 SFU
（Selective Forwarding Unit，选择性转发单元）音视频通信示例。

### 架构

```
浏览器页面A (生产者) ── WebRTC 音视频 ──┐
                                        ├─→ mediasoup SFU Server ──→ 浏览器页面B (消费者)
浏览器页面B (生产者) ── WebRTC 音视频 ──┘         │
                                                 │
                         protoo WebSocket 信令 (wss://localhost:4443)
```

### 目录结构

| 目录 | 技术栈 | 端口 | 作用 |
|------|--------|------|------|
| `server/` | Node.js (TypeScript) + mediasoup + protoo + Express | 4443 (HTTPS/WSS 信令)、44444 (TCP/UDP RTP 媒体) | SFU 服务器：接收各端音视频流并转发给其他端 |
| `app/` | React + Vite + mediasoup-client | 5555 (HTTPS) | 浏览器前端页面 |
| `broadcaster/` | TypeScript | — | 独立 FFmpeg/GStreamer 推流客户端（本次未使用） |
| `aiortc/` | Python (aiortc) | — | Python 客户端示例（本次未使用） |
| `broadcasters/` | Shell | — | FFmpeg/GStreamer 推流脚本（本次未使用） |
| `deploy/` | Shell | — | 部署脚本（本次未使用） |
| `tmp-scripts/` | Shell/TS | — | 临时测试脚本（本次未使用） |

### 通信流程

1. 两个浏览器页面各自通过 `wss://localhost:4443` 连接服务器信令（protoo WebSocket）。
2. 各自建立 WebRTC 连接，把本机摄像头/麦克风采集的音视频推送到 mediasoup SFU。
3. SFU 再把收到的流转发给房间内的其他端。
4. 接收端通过 `mediasoup-client` 消费并播放音视频。

---

## 二、环境依赖确认

### 确认结果

| 依赖 | 要求 | 本地状态 | 处理 |
|------|------|----------|------|
| Node.js | >= 22 | ✅ 已存在 v24.14.1 | 未重复安装 |
| npm | — | ✅ 已存在 11.11.0 | 未重复安装 |
| `server/node_modules` | — | ❌ 不存在 | 已安装（`npm ci`，241 包） |
| `app/node_modules` | — | ❌ 不存在 | 已安装（`npm install --legacy-peer-deps`，493 包） |
| openssl | — | ❌ 不存在 | 未使用，改用 Node 生成证书 |
| TLS 证书 | — | ❌ 不存在 | 已生成自签名证书 |

### 关键结论

- **Node.js 和 npm 已存在，未重复安装**，符合"尽量减少重复安装"的要求。
- 服务器要求 Node.js `>=22`（`server/package.json` 中 `engines.node`），本机 v24.14.1 满足。
- 前端 `urlFactory.js` 硬编码使用 `wss://` 协议 + 端口 4443，因此**服务器必须启用 TLS**。
- 服务器会校验 HTTP `Origin` 头（`Server.computeHttpOriginHeader` 生成的
  `https://localhost:4443`），且 `areSameHttpOrigins` 只比较 **protocol + hostname**，
  因此**前端页面也必须通过 HTTPS 访问**（否则 Origin 为 http 协议，会被拒绝）。

---

## 三、本次任务所有文件变更记录

### 3.1 新建文件

| 文件 | 目的 | 说明 |
|------|------|------|
| `server/config.mjs` | 服务器配置文件 | 由 `config.example.mjs` 复制而来，并配置 TLS 证书路径 |
| `server/cert.pem` | TLS 证书 | 自签名证书（localhost），有效期 365 天 |
| `server/key.pem` | TLS 私钥 | 自签名证书对应的 RSA 2048 私钥 |
| `server/generate-cert.mjs` | 证书生成脚本 | 用 `selfsigned` 包生成自签名证书 |
| `start-all.bat` | 一键启动脚本（Windows） | 自动检查依赖→启动服务器→启动前端→打开两个页面。**内容为纯英文 ASCII**（避免中文乱码问题） |
| `stop-all.bat` | 一键停止脚本（Windows） | 按窗口标题和端口停止服务。**内容为纯英文 ASCII** |
| `本地搭建与启动说明.md` | 本文档 | 记录任务全过程 |

### 3.2 修改文件

| 文件 | 修改内容 | 目的 | 结果 |
|------|----------|------|------|
| `server/src/envs.ts` | `getConfigFile()` 使用 `pathToFileURL()` 转换路径 | 修复 Windows 下 `import()` 不接受 `e:\...` 绝对路径的兼容性问题 | ✅ 修复，服务器可正常读取配置 |
| `app/src/envs.js` | `getConfigFile()` 使用 `pathToFileURL()` 转换路径 | 同上，修复前端 Vite 读取配置的同样问题 | ✅ 修复，前端可正常启动 |
| `start-all.bat` | 全文中文化为英文 ASCII，无 BOM | 修复批处理文件中文乱码导致命令解析失败的问题 | ✅ 修复，脚本可正常双击运行 |
| `stop-all.bat` | 全文中文化为英文 ASCII，无 BOM | 同上 | ✅ 修复，脚本可正常双击运行 |

### 3.3 删除文件

无。

### 3.4 未改动文件（说明）

- `server/config.example.mjs`：未改动，作为模板保留。
- 所有 `src/` 下的业务逻辑代码：未改动，仅改了上述两个 envs 文件。

---

## 四、每一步操作明细

### 步骤 1：读取项目理解

- **行为**：读取 `README.md`、`server/README.md`、`server/package.json`、`app/package.json`、`server/config.example.mjs`、`app/vite.config.ts`、`app/src/urlFactory.js`、`app/src/RoomClient.js`、`server/src/WsServer.ts`、`server/src/Server.ts`、`server/src/utils.ts`、`server/src/envs.ts`、`app/src/envs.js` 等文件。
- **目的**：理解项目功能、架构、依赖、配置、启动方式。
- **结果**：明确了 SFU 架构、端口分配、TLS 要求、Origin 校验逻辑，以及 Windows 兼容性问题。

### 步骤 2：检查环境

- **行为**：执行 `node --version`、`npm --version`，检查 `node_modules` 是否存在。
- **目的**：确认本地已有环境，避免重复安装。
- **结果**：Node v24.14.1、npm 11.11.0 已存在；两个 `node_modules` 均不存在，需要安装。

### 步骤 3：安装 server 依赖

- **行为**：`cd server && npm ci`
- **目的**：安装服务器依赖（含 mediasoup 原生模块编译）。
- **结果**：✅ 成功，安装 241 包，`prepare` 脚本自动编译了 TypeScript 到 `lib/`。

### 步骤 4：安装 app 依赖

- **行为**：`cd app && npm install --legacy-peer-deps`
- **目的**：安装前端依赖。
- **结果**：✅ 成功，安装 493 包（有 audit 警告，但不影响运行）。

### 步骤 5：生成自签名 TLS 证书

- **行为**：
  1. 检查系统 openssl → 不存在。
  2. 尝试用 Node 内置 `crypto.X509Certificate` 生成 → 该 API 不支持从 options 生成证书，失败。
  3. 安装 `selfsigned` 包（`npm install --no-save selfsigned`）。
  4. 编写 `generate-cert.mjs` 脚本，运行生成证书。
- **目的**：服务器需要 TLS（前端硬编码 wss），而本地无证书。
- **结果**：✅ 成功生成 `server/cert.pem` 和 `server/key.pem`。

### 步骤 6：创建服务器配置文件

- **行为**：
  1. `copy config.example.mjs config.mjs`。
  2. 修改 TLS 配置，把占位路径改为 `path.join(import.meta.dirname, 'cert.pem')` / `key.pem`。
  3. 添加 `import * as path from 'node:path'`。
- **目的**：提供服务器运行所需的配置，指向真实证书。
- **结果**：✅ 配置文件就绪。

### 步骤 7：修复 Windows 兼容性问题

- **行为**：修改 `server/src/envs.ts` 和 `app/src/envs.js` 的 `getConfigFile()`，用
  `pathToFileURL()` 把 Windows 绝对路径转换为 `file://` URL。
- **原因**：Node.js 的 `import()` 动态导入在 Windows 上不接受 `e:\...` 格式的路径，
  报错 `ERR_UNSUPPORTED_ESM_URL_SCHEME`。
- **结果**：✅ 修复后服务器和前端均能正常读取配置。

### 步骤 8：重新编译服务器 TypeScript

- **行为**：`cd server && npm run typescript:build`
- **目的**：让 `envs.ts` 的修改生效（编译到 `lib/`）。
- **结果**：✅ 编译成功。

### 步骤 9：启动并验证服务

- **行为**：
  1. 启动服务器 `node lib/index.js`。
  2. 验证端口：`netstat -ano | findstr "4443 5555 44444"`。
  3. 启动前端 `npm start`。
  4. 验证 HTTPS：`curl -k https://localhost:5555/` → HTTP 200；`curl -k https://localhost:4443/` → HTTP 403（正常，根路径无信令路由）。
- **结果**：✅ 服务器监听 4443（信令）+ 44444（媒体）；前端监听 5555（HTTPS），
  `npm start` 的 `runDev()` 自动打开了 producer + consumer 两个页面。

### 步骤 10：创建一键启动/停止脚本和本文档

- **行为**：创建 `start-all.bat`、`stop-all.bat`、本文档。
- **目的**：方便后续一键启动/停止。
- **结果**：✅ 完成。

### 步骤 11：修复批处理脚本中文乱码问题

- **现象**：双击 `start-all.bat` 时报错，出现
  `'cal' 不是内部或外部命令`、`'娴嬪埌' 不是内部或外部命令` 等错误，
  命令无法正常执行。
- **原因**：批处理文件最初以 **UTF-8** 编码保存，且包含中文注释和中文 `echo`
  内容。Windows 的 `cmd.exe` 默认用 **GBK/ANSI** 编码读取批处理文件，导致：
  1. 中文内容变成乱码（如"检测到"→"娴嬪埌"）。
  2. 乱码破坏了命令解析，`call` 被截断成 `cal` 等，`echo` 后的中文被当成命令执行。
- **解决**：将 `start-all.bat` 和 `stop-all.bat` 的内容全部改为**纯英文（ASCII）**。
  ASCII 字符在 UTF-8 和 ANSI 编码下完全一致，从根源上杜绝乱码。
- **验证**：检查文件前 3 字节为 `64,101,99`（即 `@ec`，`@echo` 开头），
  确认**无 BOM**、第一行 `@echo off` 干净无误。
- **结果**：✅ 修复，脚本可正常双击运行。

> **提示**：如需在 Windows 批处理中使用中文，需将 `.bat` 文件保存为 **GBK/ANSI**
> 编码，或使用英文。UTF-8 编码的批处理在默认 `cmd.exe` 下会乱码。

---

## 五、后续如何启动

### 方式一：一键启动（推荐）

双击运行项目根目录下的 **`start-all.bat`**，脚本会自动：

1. 检查 Node.js。
2. 检查并（必要时）安装依赖（已存在则跳过）。
3. 检查并（必要时）生成 TLS 证书。
4. 启动服务器（新窗口）。
5. 等待服务器就绪后启动前端（新窗口），自动打开两个浏览器页面。

**停止**：运行 **`stop-all.bat`**。

### 方式二：手动启动（两个终端）

```bat
REM 终端 1 —— 启动 SFU 服务器
cd e:\gap\mediasoup-demo\server
set DEBUG=mediasoup-demo-server* *INFO* *WARN* *ERROR*
node lib/index.js

REM 终端 2 —— 启动前端
cd e:\gap\mediasoup-demo\app
npm start
```

### 访问页面

- 前端地址：`https://localhost:5555/`
- `npm start` 会自动打开两个页面：
  - **producer 页面**：采集摄像头/麦克风并发布。
  - **consumer 页面**：只订阅播放（适合单摄像头/单麦克风测试）。
- 也可以手动打开两个 `https://localhost:5555/` 页面，并让它们进入**同一房间**
  （`roomId` 相同）即可互相通信。

### 首次访问注意事项

1. 证书是**自签名**的，浏览器会提示不安全，需点击"高级 → 继续访问"。
2. 需要允许浏览器访问**摄像头和麦克风**权限。
3. 两个页面需在**同一房间**（`roomId` 一致）。

---

## 六、常见问题：只有一个摄像头和一个麦克风，能否两个页面通话？

### 问题描述

> 只有一个摄像头和一个麦克风，是否可以同时打开两个浏览器页面进行音视频通话？

### 回答：可行

**结论：完全可行。** 浏览器允许同一个物理摄像头/麦克风被多个标签页同时访问
（Windows/macOS/Linux 的浏览器均支持多标签页共享采集设备），因此单个摄像头和
单个麦克风足以完成两个页面的音视频通信测试。

不过需要注意：两个页面**同时采集同一个设备**时会有并发争抢和回声问题，
因此推荐采用"一采一收"的方式来测试，详见下方三种方式。

### 三种测试方式对比

| 方式 | 页面A 角色 | 页面B 角色 | 是否争抢设备 | 适用场景 |
|------|-----------|-----------|-------------|---------|
| **dev 模式（自动，推荐）** | producer（只采集发布） | consumer（只订阅播放） | 否 | 最省事，验证完整链路 |
| **手动指定角色** | `?produce=true&consume=false`（只发不收） | `?produce=false&consume=true`（只收不发） | 否 | 需要精确控制时 |
| **完全双向** | 都采都收 | 都采都收 | 是（共享） | 验证双向并发场景 |

### 详细说明

1. **dev 模式（自动，推荐）**

   `npm start` 会自动打开两个页面，`vite.config.ts` 中的 `runDev()` 已为
   producer 页面设置 `consume=false`、consumer 页面设置 `produce=false`：

   - **producer 页面**：采集摄像头/麦克风，只发布（推送）音视频。
   - **consumer 页面**：不采集，只订阅（接收）并播放 producer 的音视频。

   两个页面不争抢设备，可以完整验证「采集 → 编码 → 推流 → SFU 转发 → 接收 → 播放」
   的整条链路。

2. **手动指定角色**

   手动打开两个页面，通过 URL 查询参数控制角色：

   - 页面A（只发不收）：`https://localhost:5555/?roomId=test&produce=true&consume=false`
   - 页面B（只收不发）：`https://localhost:5555/?roomId=test&produce=false&consume=true`

   **注意**：两个页面的 `roomId` 必须一致（上例中都为 `test`）才能互相通信。

3. **完全双向（默认双角色）**

   两个页面都使用 `?roomId=xxx`（不带 `produce`/`consume` 参数，默认两者都启用），
   各自都能采+收，可以实现**真正的双向视频通话**。

   - **回声/啸叫**：两个页面同时采集麦克风又同时外放，声音会循环放大，产生啸叫。
     **必须佩戴耳机**来避免。
   - **采集延迟**：共享设备会引入轻微的采集争抢延迟。

   > ⚠️ **重要提示：完全双向必须用两个不同的浏览器**
   >
   > 如果用**同一个浏览器**（如两个 Chrome 标签页），受 Chrome 同源标签页共享
   > 设备策略影响，**第二个标签页常常拿不到摄像头/麦克风**（表现为对方页面
   > 显示 `resolution: 0x0`、`producerScore: 0`、空黑屏）。
   >
   > **推荐做法**：
   > - 用 **Chrome + Edge** 或 **Chrome + Firefox** 分别打开两个页面。
   > - 验证双向通信后，再换用 `produce=false` 单角色方式做单摄像头测试。

### 相关查询参数说明

| 参数 | 作用 | 取值 |
|------|------|------|
| `roomId` | 房间 ID，两个页面需一致 | 任意字符串 |
| `produce` | 是否发布（采集并推送）音视频 | `true` / `false` |
| `consume` | 是否订阅（接收并播放）音视频 | `true` / `false` |
| `mic` | 是否默认开启麦克风 | `true` / `false` |
| `webcam` | 是否默认开启摄像头 | `true` / `false` |

---

## 七、已知问题与说明

1. **Windows 路径兼容性问题**：原项目 `envs.ts` / `envs.js` 的 `getConfigFile()`
   返回 Windows 绝对路径（如 `e:\...`），Node.js `import()` 不支持，已用
   `pathToFileURL()` 修复。
2. **批处理脚本中文乱码问题**：`start-all.bat` / `stop-all.bat` 最初用 UTF-8
   编码保存且含中文，`cmd.exe` 按 GBK 读取导致乱码、命令解析失败。已改为
   纯英文 ASCII 修复。**今后编辑 `.bat` 脚本时，应保持英文，或用 GBK/ANSI 编码。**
3. **同一浏览器多标签页共享设备失败**：Chrome 等浏览器对同源的多个标签页共享
   摄像头/麦克风有限制，**第二个标签页常常拿不到设备**，表现为对方画面是黑屏、
   `resolution: 0x0`、`producerScore: 0`。**解决方法：用两个不同的浏览器**（如
   Chrome + Edge）。详见第六节"完全双向"说明。
4. **npm audit 警告**：`app` 安装时有 19 个 audit 警告（eslint 8.x 等 dev 依赖），
   不影响运行。
5. **自签名证书**：浏览器需手动信任，生产环境应使用正式 CA 签发的证书。
6. **防火墙**：如局域网内其他设备访问，需放行 4443、44444、5555 端口。
