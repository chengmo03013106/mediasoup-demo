# mediasoup-demo 项目架构与数据流文档

> 本文档基于 `mediasoup-demo` v3 分支源码整理，涵盖项目功能、依赖、技术栈、
> 整体架构，以及两个核心问题的深入解答：
> 1. mediasoup 的 C++ 模块与 TypeScript 服务端如何交互；
> 2. 一次媒体音视频数据全流程；
> 3. 一次信令服务数据全流程。

---

## 目录

1. [项目概述](#一项目概述)
2. [项目实现的功能](#二项目实现的功能)
3. [项目依赖](#三项目依赖)
4. [技术栈](#四技术栈)
5. [整体架构](#五整体架构)
6. [mediasoup C++ 与 TypeScript 的交互机制](#六mediasoup-c-与-typescript-的交互机制)
7. [媒体音视频数据全流程](#七媒体音视频数据全流程)
8. [信令服务数据全流程](#八信令服务数据全流程)

---

## 一、项目概述

**mediasoup-demo** 是 [mediasoup](https://mediasoup.org) 官方提供的**演示应用**（v3 分支），
用于展示基于 **SFU（Selective Forwarding Unit，选择性转发单元）** 架构的多方音视频实时通信。

- **许可证**：ISC
- **在线演示**：https://v3demo.mediasoup.org
- **核心价值**：完整演示 mediasoup 的 SFU 架构，以及浏览器（React）、Node.js 服务端、
  以及 FFmpeg/GStreamer/Python 等非浏览器客户端的接入方式。

---

## 二、项目实现的功能

### 核心功能（音视频通信）

| 功能 | 说明 |
|------|------|
| 多方音视频会议 | 多用户加入同一房间，互相收发音视频（SFU 转发） |
| 音频通信 | 麦克风采集 → OPUS 编码 → SFU 转发（支持双声道） |
| 旁路降噪（in-line processing） | 可选开启：将 speaker 的音频流旁路一份推给自研降噪服务，降噪服务以 BroadcasterPeer 身份回传处理结果（详见 `server/docs/降噪旁路项目开发文档.md`） |
| 视频通信 | 摄像头采集，支持 VP8 / VP9 / H264 / AV1 多编码 |
| 屏幕共享 | `getDisplayMedia` 采集屏幕，支持 4K |
| Simulcast / SVC | 视频分层（空间层/时间层），自适应带宽 |
| 数据通道 | SCTP DataChannel 聊天、Bot 机器人、文件拖拽 |
| 网络限速 | `@sitespeed.io/throttle` 模拟带宽限制（需 secret） |
| 端到端加密 | E2E insertable streams（仅部分 OPUS/VP8 编码） |
| 人脸检测 | `face-api.js` 可选人脸检测 |
| ICE restart | 断线重连 |
| rtcstats 上报 | 接入 `@rtcstats/rtcstats-js` 统计追踪 |

### 辅助功能

- **交互式调试终端**：通过 Unix socket + REPL 运维调试（dump 对象、查看 stats、改日志级别）。
- **HTTP API**：供非浏览器客户端（BroadcasterPeer，如 FFmpeg/GStreamer）通过 REST 接入。
- **内置 Bot**：处理聊天 DataChannel 的机器人。
- **40+ URL 查询参数**：控制房间、编码、simulcast、测试等行为。

---

## 三、项目依赖

### 服务端（`server/`）

| 依赖 | 版本 | 用途 |
|------|------|------|
| mediasoup | ^3.24.2 | SFU 核心库（含 C++ worker） |
| protoo-server | ^4.0.8 | WebSocket 信令协议 |
| express | ^5.2.1 | HTTP API 服务 |
| body-parser | ^2.3.0 | HTTP 请求体解析 |
| debug | ^4.4.3 | 日志系统 |
| @sitespeed.io/throttle | ^6.0.1 | 网络限速模拟 |
| awaitqueue | ^3.3.1 | 异步任务串行化（防竞态） |
| pidusage | ^4.0.1 | 进程 CPU/内存监控 |
| picocolors / supports-color | — | 终端彩色输出 |

**运行时要求**：Node.js `>=22`。

### 前端（`app/`）

| 依赖 | 版本 | 用途 |
|------|------|------|
| react / react-dom | 18.3.1 | UI 框架 |
| redux / react-redux | 4.0.5 / 7.2.0 | 状态管理 |
| redux-thunk | 2.3.0 | 异步 action |
| mediasoup-client | ^3.22.0 | 浏览器端 WebRTC 媒体核心 |
| protoo-client | 4.0.8 | 信令 WebSocket 客户端 |
| vite | 6.2.1 | 构建工具 |
| sass | 1.98.0 | 样式 |
| bowser / hark / face-api.js | — | UA 检测 / 音量检测 / 人脸检测 |
| @rtcstats/rtcstats-js | ^2.2.2 | WebRTC 统计追踪 |

### 其他模块

| 模块 | 技术栈 | 用途 |
|------|--------|------|
| broadcaster/ | Node.js (TS) + mediasoup-client | 非浏览器推流客户端（未完成） |
| aiortc/ | Python aiortc + mediasoup-client-aiortc | Python 客户端示例（未适配） |
| broadcasters/ | Shell | FFmpeg/GStreamer 推流脚本 |
| deploy/ | Shell | 部署脚本 |

---

## 四、技术栈

| 层 | 技术 |
|----|------|
| 服务端语言 | TypeScript（编译到 JS 运行于 Node.js >=22） |
| 媒体核心 | mediasoup（Node.js API + C++ worker 子进程） |
| 信令协议 | protoo（WebSocket） |
| HTTP API | Express 5 |
| 前端框架 | React 18 + Redux 4 + redux-thunk |
| 构建工具 | Vite 6 + @vitejs/plugin-react |
| 样式 | Sass |
| 日志 | debug 库 |
| 测试/质量 | ESLint + Prettier + nodemon + tsx |

---

## 五、整体架构

### 5.1 模块划分（Monorepo）

```
mediasoup-demo/
├── server/          # SFU 服务端（Node.js + TypeScript）—— 核心
├── app/             # 浏览器前端（React + Vite）—— 核心
├── broadcaster/     # 非浏览器推流客户端（未完成）
├── aiortc/          # Python 客户端示例（未适配）
├── broadcasters/    # FFmpeg/GStreamer 推流脚本
├── deploy/          # 部署脚本
└── tmp-scripts/     # 临时测试脚本
```

### 5.2 系统架构图

```
┌───────────────────────────────────────────────────────────────┐
│                        浏览器 (app/)                           │
│  ┌──────────────────────┐        ┌──────────────────────┐     │
│  │  Peer A (React+Redux)│        │  Peer B (React+Redux)│     │
│  │  mediasoup-client    │        │  mediasoup-client    │     │
│  │  protoo-client       │        │  protoo-client       │     │
│  └──────────┬───────────┘        └──────────┬───────────┘     │
└─────────────┼───────────────────────────────┼─────────────────┘
              │ wss://localhost:4443 (信令)    │
              │ SRTP/RTP :44444+ (媒体)        │
┌─────────────▼───────────────────────────────▼─────────────────┐
│                     mediasoup SFU 服务端 (server/)             │
│                                                               │
│  ┌─────────────┐ ┌─────────────┐ ┌──────────────────┐         │
│  │  WsServer   │ │  ApiServer  │ │ TerminalServer   │         │
│  │  (protoo WS)│ │  (Express)  │ │ (调试终端)       │         │
│  └──────┬──────┘ └──────┬──────┘ └──────────────────┘         │
│         └────────┬──────┘                                     │
│             ┌────▼────┐                                       │
│             │  Server │ (Worker/WebRtcServer/Room 编排)        │
│             └────┬────┘                                       │
│        ┌─────────┼─────────┐                                  │
│   ┌────▼───┐┌────▼───┐┌────▼────┐                             │
│   │ Room 1 ││ Room 2 ││ Room N  │                             │
│   └────────┘└────────┘└─────────┘                             │
│                                                               │
│  mediasoup Workers (C++ 子进程 x N) + WebRtcServer + Router   │
└───────────────────────────────────────────────────────────────┘
```

### 5.3 服务端类层级

```
index.ts (进程入口：加载配置、启动、信号处理)
  └── Server (全局单例)
        ├── 管理 N 个 mediasoup Worker 子进程 + WebRtcServer（轮询分配）
        ├── WsServer   → protoo WebSocket 信令（浏览器 Peer）
        ├── ApiServer  → Express HTTP API（BroadcasterPeer）
        ├── TerminalServer → 调试终端
        └── Room（按 roomId 懒创建，AwaitQueue 防竞态）
              ├── Peer            （浏览器客户端）
              ├── BroadcasterPeer （HTTP API 客户端）
              ├── Bot             （聊天机器人）
              └── MediaRelay      （旁路降噪，可选，processing.enabled 时创建）
```

**关键机制**：
- **多 Worker 进程**：`numWorkers` 由配置决定（默认 = CPU 核数），每个 Worker 独立
  WebRtcServer（端口按 idx 递增，默认 44444 起），轮询分配。
- **双 Router**：每个 Room 创建 `producerRouter` 和 `consumerRouter`；若
  `usePipeTransports=true`，两者分属不同 Worker，用 `pipeToRouter` 跨进程转发。

### 5.4 前端架构

```
index.jsx（createRoot + Redux Provider + RoomContext.Provider）
  └── <Room />（顶层容器，didMount 调用 roomClient.join()）
        ├── <Notifications />
        ├── <Peers /> → <Peer /> → <PeerView />   （远端 peers 媒体展示）
        ├── <Me /> → <PeerView isMe />            （本地媒体 + 控制按钮）
        ├── <ChatInput />                          （聊天/Bot）
        ├── <Stats />                              （WebRTC 统计）
        └── <NetworkThrottle />                    （网络限速）
```

**关键机制**：
- **RoomClient.js**（约 2725 行）：前端逻辑中枢，封装信令（protoo）、mediasoup
  Device、send/recv transport、produce/consume、数据通道、E2E、ICE restart 等全部逻辑。
- **Redux** 管理状态：room / me / peers / producers / consumers /
  dataProducers / dataConsumers / notifications。
- **RoomClient 通过 React Context 传递**（非 Redux），`withRoomContext` HOC 注入。
- **连接地址**：`urlFactory.js` 固定 `wss://${hostname}:4443`。

---

## 六、mediasoup C++ 与 TypeScript 的交互机制

### 6.1 核心结论

mediasoup 是一个**混合语言**的库：
- **C++ 部分**：`mediasoup-worker`（独立的可执行文件），承载真正重负载的媒体处理
  （RTP 收发、转发、simulcast、BWE 等）。
- **Node.js/TypeScript 部分**：`node/lib/` 下的 API 层，提供 `Worker`/`Router`/
  `Transport`/`Producer`/`Consumer` 等 JS 对象。

两者通过 **Node.js 主进程 spawn 独立的 C++ worker 子进程** 的方式协同，使用
**一对 libuv 管道（pipe）** 通信，协议是 **FlatBuffers 二进制**（版本 3.24.x）。

### 6.2 进程模型

```
Node.js 主进程 (mediasoup-demo-server)
   │
   │  child_process.spawn()
   ▼
mediasoup-worker 子进程 (C++ 可执行文件)
   路径: worker/out/Release/mediasoup-worker.exe
```

- Node 端通过 `Worker.js` 的 `spawn()` 启动 worker 子进程（`node/lib/Worker.js:122-150`）。
- 每个 mediasoup Worker 对应一个独立的 C++ 子进程。

### 6.3 IPC 通道：一对 libuv 管道（fd 3 / fd 4）

**不是 node-ipc，也不是独立 socket 文件**，而是通过 `spawn` 的 `stdio` 传递的
**文件描述符**（Unix 上是 libuv `uv_pipe_t`，Windows 上映射为命名管道）。

Node 端 `Worker.js` 的 spawn 配置：

```js
this.#child = spawn(spawnBin, spawnArgs, {
    stdio: ['ignore', 'pipe', 'pipe', 'pipe', 'pipe'], // fd 0~4
});
// fd 3 = producer channel（Node → worker，发 request/notification）
// fd 4 = consumer channel（worker → Node，收 response/notification/log）
this.#channel = new Channel({
    producerSocket: this.#child.stdio[3],
    consumerSocket: this.#child.stdio[4],
});
```

C++ 端 `worker/src/main.cpp` 硬编码对应 fd：

```cpp
static constexpr int ConsumerChannelFd{ 3 };  // 注意：C++ 视角的"接收"是 fd 3
static constexpr int ProducerChannelFd{ 4 };
```

C++ 端用 libuv `uv_pipe_init` + `uv_pipe_open` 打开这些 fd
（`worker/src/handles/UnixStreamSocketHandle.cpp:92-109`）。

> 两个方向的 fd 是**独立的单向管道**（不是单一双向 socket）。

### 6.4 通信协议：FlatBuffers 二进制

**重要**：3.24.x 版本已把旧版 JSON 协议彻底替换为 **FlatBuffers**。

- 依赖：`flatbuffers`（`node/lib/Channel.js:39`）。
- 协议 schema 定义在 `worker/fbs/*.fbs`（`message.fbs`、`request.fbs`、
  `response.fbs`、`notification.fbs`）。

消息外层结构（`worker/fbs/message.fbs`）：

```fbs
union Body {
    Request, Response, Notification, Log
}
table Message { data: Body (required); }
root_type Message;
```

**帧格式**：`4 字节小端长度前缀 + FlatBuffer 负载`（`MESSAGE_MAX_LEN = 8MB`）。

### 6.5 序列化 / 反序列化

**Node 发送**（`Channel.js`）：
- `request()` / `notify()` 用 `flatbuffers.Builder` 构建消息，调用
  `finishSizePrefixed()` 自动加 4 字节长度前缀，写入 `producerSocket`。

**Node 接收**（`Channel.js:73-135`）：
- `consumerSocket.on('data')` 累积到 buffer，读 4 字节长度前缀 → 切出负载 →
  `Message.getRootAsMessage()` 反序列化 → 按 `dataType()` 分发到
  `processResponse` / `processNotification` / `processLog`。

**C++ 接收**（`worker/src/Channel/ChannelSocket.cpp`）：
- `ConsumerSocket::UserOnUnixStreamRead()` 循环解析「长度前缀 + 负载」帧，
  逐条调用 `OnConsumerSocketMessage()`。

### 6.6 方法调用（request / response 模式）

**Node 端调用 worker 方法**（`Channel.js:217-273`）：

```js
request(method, bodyType, bodyOffset, handlerId) {
    const id = this.#nextId++;          // 自增 id
    // 构建 Request{ method, handlerId, body }，finishSizePrefixed 后写入
    // 返回 Promise，将 { id, resolve, reject } 存入 #sents Map
}
```

- 收到匹配 id 的 `Response` 时，`processResponse()` 根据 `accepted`/`error` 字段
  resolve 或 reject。

**C++ 端处理**（`worker/src/Worker.cpp:238-364`）：
- `HandleRequest()` 用 `switch (request->method)` 分派：
  - worker 级方法（`WORKER_DUMP`、`WORKER_CREATE_ROUTER` 等）直接处理；
  - 其他方法走 `default` 分支，通过 `ChannelMessageRegistrator::GetChannelRequestHandler(handlerId)`
    路由到对应的 Router/Transport 等对象。
- `handlerId` 机制：每个 C++ 对象创建时注册自己的 id，Node 端在 request 中带上
  `handlerId`，worker 即可路由到正确对象。

**C++ 回传结果**（`worker/src/Channel/ChannelRequest.cpp`）：
- `Accept()` 构建 `Response{accepted=true}`；`Error()`/`TypeError()` 构建错误响应。
- `SendResponse()` 包一层 `Message`，`FinishSizePrefixed` 后通过 producer fd 写回。

### 6.7 事件回传（notification 模式）

**C++ 端**（`worker/src/Channel/ChannelNotifier.cpp:16-28`）：

```cpp
auto notification = FBS::Notification::CreateNotificationDirect(builder, targetId.c_str(), event);
auto message = FBS::Message::CreateMessage(builder, ..., notification.Union());
builder.FinishSizePrefixed(message);
this->channel->Send(...);
```

- 例如 worker 启动后立即 `Emit(Logger::Pid, WORKER_RUNNING)` 通知 Node 端"已就绪"。

**Node 端**（`Channel.js:304-312`）：

```js
setImmediate(() => this.emit(notification.handlerId(), notification.event(), notification));
```

- 用 `handlerId` 作为事件名、`event` 枚举作为参数 `emit`。

### 6.8 交互机制总结表

| 维度 | 结论 |
|------|------|
| 进程模型 | Node 主进程 spawn C++ worker 子进程 |
| IPC 通道 | libuv `uv_pipe_t`（fd 3 / fd 4，两个独立单向管道） |
| 通信协议 | FlatBuffers 二进制（3.24.x 已弃用旧 JSON 协议） |
| 帧格式 | 4 字节小端长度前缀 + FlatBuffer 负载 |
| Node → worker | `Channel.request()`（带自增 id）→ Promise 等待匹配 Response |
| worker → Node | `ChannelNotifier::Emit()` → Node `emit(handlerId, event)` |
| 对象路由 | `handlerId`（C++ 对象注册 id，Node 请求携带以路由） |

---

## 七、媒体音视频数据全流程

> 场景：Peer A（浏览器）开启摄像头，Peer B（浏览器）观看。以下为 RTP 包的完整路径。

### 阶段一：浏览器采集（app 端）

**`app/src/RoomClient.js`**

1. **麦克风采集**（`enableMic()`，行 964-1070）：

```js
const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
track = stream.getAudioTracks()[0];
```

2. **摄像头采集**（`enableWebcam()`，行 1112-1322）：

```js
const stream = await navigator.mediaDevices.getUserMedia({
    video: { deviceId: { ideal: device.deviceId }, ...WEBCAM_VIDEO_CONSTRAINS[resolution] },
});
track = stream.getVideoTracks()[0];
```

3. **屏幕共享采集**（`enableShare()`，行 1476-1695）：
   `navigator.mediaDevices.getDisplayMedia(...)`。

### 阶段二：客户端 produce（app 端）

**`app/src/RoomClient.js`**

4. 采集到的 track 交给 `_sendTransport.produce()`：
   - 麦克风：行 1019-1027（`appData.source = 'audio'`）
   - 摄像头：行 1266-1275（`appData.source = 'video'`）

```js
this._micProducer = await this._sendTransport.produce({
    track, codecOptions, headerExtensionOptions, codec,
    appData: { source: 'audio' },
});
```

5. `_sendTransport` 的 `produce` 事件（行 2448-2465）通过 protoo 请求信令发给服务器：

```js
this._sendTransport.on('produce', async ({ kind, rtpParameters, appData }, callback, errback) => {
    const { producerId } = await this._protoo.request('produce', {
        transportId: this._sendTransport.id, kind, rtpParameters, appData,
    });
    callback({ id: producerId });
});
```

> 此处的 `produce` 是**信令**（通过 WebSocket 走 4443 端口），不是媒体数据。
> 真正的媒体 RTP 包在 ICE/DTLS 握手完成后，通过 WebRTC 连接直连 mediasoup-worker。

### 阶段三：服务端处理 produce 请求（server 端）

**`server/src/Peer.ts`**，`handleProtooRequest()` 的 `case 'produce'`（行 869-894）：

```ts
const transport = this.assertAndGetWebRtcTransport(transportId);
const producer = await transport.produce({
    kind, rtpParameters,
    appData: { peerId: this.id, source },
});
this.#producers.set(producer.id, producer);
this.handleProducer(producer);
this.emit('new-producer', { producer });   // 通知 Room
accept({ producerId: producer.id });
```

### 阶段四：Room 转发给其他 peer（server 端）

**`server/src/Room.ts`**，`new-producer` 事件（行 596-622）：

```ts
peer.on('new-producer', async ({ producer }) => {
    if (this.#usePipeTransports) {
        await this.#producerRouter.pipeToRouter({ producerId: producer.id, router: this.#consumerRouter });
    }
    for (const otherPeer of this.getOtherPeers(peer)) {
        void otherPeer.consume({ producer, consumerReplicas: this.#consumerReplicas });
    }
});
```

- `getOtherPeers(peer)` 过滤掉 producer 本人。
- 对每个其他 peer（这里是 Peer B）调用 `consume()`。接收端注册都Router。

### 阶段五：服务端创建 Consumer（server 端）

**`server/src/Peer.ts`**，`consume()→ transport.consume({ producerId, rtpCapabilities, enableRtx:true, paused:true, ignoreDtx:true })`（行 271-387）：

```ts
// 1. 检查是否能消费（canConsume）
this.emit('get-can-consume', { producerId, rtpCapabilities }, _canConsume => { ... });

// 2. 在 B 的 consumer transport 上创建 Consumer（paused 模式）
consumer = await transport.consume({
    producerId: producer.id,
    rtpCapabilities: this.#rtpCapabilities,
    enableRtx: true, paused: true, ignoreDtx: true,
    appData: { peerId: producer.appData.peerId, source: producer.appData.source },
});

// 3. 发 newConsumer 请求给 B 的浏览器
await this.request('newConsumer', {
    peerId, transportId, consumerId, producerId, kind,
    rtpParameters, type, producerPaused, ...
});
```

### 阶段六：客户端接收并播放（app 端）

**`app/src/RoomClient.js`**，处理 `newConsumer` 请求（行 395-466）：

```js
case 'newConsumer': {
    const consumer = await this._recvTransport.consume({
        id: consumerId, producerId, kind, rtpParameters, ...
    });
    this._consumers.set(consumer.id, consumer);
    // 挂载 consumer.track 到 <video>/<audio> 元素（通过 Redux → PeerView）
}
```

**`app/src/components/PeerView.jsx`**（行 565-592）：`_setTracks()` 把 track 挂到
`videoElem.srcObject` / `audioElem.srcObject` 并播放。

### 媒体数据 RTP 包的实际传输路径（关键）

**媒体数据不经过 Node.js 主进程！** 这是 SFU 架构的核心：

```
Peer A 浏览器 ──SRTP/RTP(UDP/TCP :44444+)──▶ mediasoup-worker (C++ 进程)
                                                  │
                                   Worker 内部 Router 直接转发
                                                  │
                                              Peer B 浏览器 ◀──SRTP/RTP──
```

从「发送方浏览器 → mediasoup-worker(C++) → 接收方浏览器」，Node 进程不在媒体路径上。

浏览器A (发送)                      mediasoup-worker (C++ 子进程)                 浏览器B (接收)
  sendTransport ──SRTP/UDP:44444──▶ WebRtcTransport(producer) ─▶ Router ─▶ Producer
                                                                      │ 自动转发
  recvTransport ◀──SRTP/UDP:44444── WebRtcTransport(consumer) ◀─ Consumer ◀─┘

关键区别：信令走 WebSocket 4443（Node 处理），媒体走 44444+ 的 UDP/TCP（C++ worker 处理），两条完全不同的链路。


- 端口配置在 `server/config.mjs` 的 `mediasoup.webRtcServerOptions.listenInfos`
  （默认 44444 起，UDP + TCP）。
- Node.js 主进程只负责**信令**（protoo/HTTP），媒体 RTP 包由 C++ worker 直接收发转发，
  **绕开 Node 主进程**，这就是 mediasoup 高性能的关键。

#### 自动转发
  mediasoup 的 Router 是一个 C++ 实现的 RTP 转发器。所谓"自动"，是指：Node 代码从不搬运任何 RTP 包，它只干两件编排的事：

  - 发送方 produce 时，把 RTP 流"注册"成一个 Producer（挂在 Router 上）；
  - 接收方 consume 时，在同一个 Router 上建一个 Consumer，Consumer 用 producerId 指向那个 Producer。

  一旦 Consumer 建立，Router 在 C++ 层就会把 Producer 收到的 RTP 包自动复制/转发给所有指向它的 Consumer。Node 不需要也不参与转发，这就是"自动"的真正含义。


---

## 八、信令服务数据全流程

> 场景：Peer A 从打开页面到成功加入房间。信令走 protoo 协议（WebSocket，端口 4443）。

### 阶段一：浏览器生成身份并建立连接（app 端）

**`app/src/index.jsx`**：

1. 生成身份（行 61-63）：

```js
const peerId = randomString({ length: 8 }).toLowerCase();
let roomId = urlParser.query.roomId;  // 从 URL 取 roomId
```

2. 创建 `RoomClient` 并调用 `join()`（`Room.jsx` 的 `componentDidMount`）。

**`app/src/RoomClient.js`**：

3. 构造 protoo URL（行 238-244，结合 `urlFactory.js`）：

```js
// urlFactory.js 固定 wss://${hostname}:4443
this._protooUrl = getProtooUrl({ roomId, peerId, ... });
this._protoo = new protooClient.Peer(new protooClient.WebSocketTransport(this._protooUrl));
```

4. WebSocket 连接建立（`open` 事件触发 `_joinRoom()`）。

### 阶段二：服务端接收连接（server 端）

**`server/src/WsServer.ts`**，`connectionrequest` 回调（行 79-149）：

5. **校验 Origin**（行 81-90）：`areSameHttpOrigins` 校验请求 Origin 是否与配置一致。
6. **解析参数**（行 101-102）：

```ts
const roomId = params.get('roomId');
const peerId = params.get('peerId');
```

7. **触发房间创建**（行 123-130）：`this.emit('get-or-create-room', { roomId, ... }, resolve, reject)`。

**`server/src/Server.ts`**：

8. `getOrCreateRoom()`（行 323-413）：按 `roomId` 懒创建 Room（首次），用 `AwaitQueue`
   串行化避免并发竞态。首次创建时打印
   `getOrCreateRoom() | creating a new Room [roomId:xxx]`。

9. `Room.processWsConnection()`（`server/src/Room.ts` 行 288-313）：

```ts
const protooPeer = this.#protooRoom.createPeer(peerId, protooTransport);
const peer = Peer.create({ peerId, protooPeer, remoteAddress });
this.#joiningPeers.set(peer.id, peer);  // 先放入 joining 集合
this.handlePeer(peer);
```

### 阶段三：join 请求处理（server 端）

**`server/src/Peer.ts`**，`case 'join'`（行 797-816）：

10. Peer 收到客户端的 `join` 请求（携带 displayName、device、rtpCapabilities）。
11. 校验通过后 `emit('joined')`，`accept({ peers: ... })` 返回房间里已有的 peer 列表。

**`server/src/Room.ts`**，`joined` 事件（行 468-505）：

12. 把 peer 从 `#joiningPeers` 移到 `#peers`。
13. 通知房间内**其他 peer** `newPeer`（让它们知道有新成员）。
14. 遍历新 peer 能消费的其他 peer 的 producer，触发 `consume()`。

### 阶段四：客户端完成加入（app 端）

**`app/src/RoomClient.js`**，`_joinRoom()`（行 2337-2640）：

15. 加载 `routerRtpCapabilities`（行 2368-2375）。
16. 创建 send/recv transport（`createWebRtcTransport` 请求）。
17. 发送 `join` 请求（行 2559-2565）。
18. 成功后 `setRoomState('connected')`（行 2567），UI 提示 "You are in the room!"。

### 信令消息格式（protoo）

protoo 消息分两类：

```jsonc
// 请求（request）—— 需要对方响应
{ "request": true, "id": 1, "method": "join", "data": { ... } }
// 响应（response）
{ "response": true, "id": 1, "ok": true, "data": { ... } }

// 通知（notification）—— 无需响应
{ "notification": true, "method": "newPeer", "data": { ... } }
```

### 信令数据流时序图

```
Peer A (浏览器)                       Server (Node.js)
     │  WebSocket 连接 (wss://localhost:4443)
     │──────────────────────────────────────▶│  WsServer 校验 Origin、解析 roomId/peerId
     │                                        │  get-or-create-room → Room
     │                                        │  processWsConnection → Peer
     │  request: getRouterRtpCapabilities     │
     │──────────────────────────────────────▶│
     │  ◀────────────────────────────────────│  response: routerRtpCapabilities
     │  request: createWebRtcTransport        │
     │──────────────────────────────────────▶│
     │  ◀────────────────────────────────────│  response: transport params
     │  request: join {displayName, device}   │
     │──────────────────────────────────────▶│  Peer 处理 join → emit joined
     │                                        │  Room: joiningPeers → peers
     │                                        │  Room: 通知其他 peer newPeer
     │  ◀────────────────────────────────────│  response: join ok { peers: [...] }
     │  setRoomState('connected')             │
```

---

## 附：关键端口汇总

| 端口 | 协议 | 用途 |
|------|------|------|
| 4443 | HTTPS/WSS | 信令服务（protoo WebSocket） |
| 44444+ | UDP/TCP（RTP/SRTP） | 媒体传输（WebRtcServer，按 Worker 递增） |
| 5555 | HTTPS | 前端 Vite 开发服务器 |
| 40000-40999 | UDP | PlainTransport（FFmpeg/GStreamer 推流） |

---

## 附：关键技术结论

1. **SFU 架构核心**：媒体数据（RTP）由 C++ `mediasoup-worker` 直接收发转发，
   **不经过 Node.js 主进程**；Node.js 只处理信令。
2. **Node ↔ C++ 交互**：`spawn` 子进程 + 一对 libuv 管道（fd 3/4）+ FlatBuffers 二进制协议。
3. **信令协议**：protoo（WebSocket），浏览器端 protoo-client、服务端 protoo-server。
4. **前端核心**：RoomClient.js 封装全部媒体与信令逻辑，Redux 管理状态，React 渲染 UI。

5. RTCP 同理：NACK/PLI/TWCC/RR 都在 worker 内部处理，走的是和 RTP 同一条 SRTP 通道，Node 代码里搜不到任何 RTCP 收发。

---

## 九、旁路降噪数据流（in-line processing，可选）

> 这是在本项目之上新增的改造能力，用于把 speaker 的音频流旁路一份推给自研降噪
> 服务做学习/调试。默认 `processing.enabled = false` 时不启用，走原有直连路径。

### 9.1 数据流

```
[processing.enabled = false]（默认，原始直连）
  speaker ──producer──▶ SFU ──consumer──▶ 其他参会者

[processing.enabled = true]（旁路降噪）
  speaker ──producer──▶ SFU ──consumer──────────▶ 其他参会者（原始流，保留）
                            │
                            └─ consumer(旁路) ──▶ PlainTransport(出) ──▶ 降噪服务
                                                                        │
  其他参会者 ◀──consumer── SFU ◀──producer(回传) ◀── BroadcasterPeer "denoiser" ◀──┘
                     (降噪流，独立 peer 条目，供 A/B 对比)
```

### 9.2 关键机制

| 环节 | 实现 | 代码位置 |
|------|------|----------|
| 旁路模块 | `MediaRelay`，在 `producerRouter` 上建出方向 PlainTransport，`connect` 到降噪服务 | `server/src/MediaRelay.ts` |
| 旁路触发 | `Room` 收到 `new-producer`，若 `producer.kind === 'audio'` 则 `mediaRelay.bypass(producer)` | `Room.ts` `peer.on('new-producer')` 分支 |
| 旁路 consumer | 从 producer 的 opus codec 动态构造 rtpCapabilities，`ignoreDtx:false`、`enableRtx:false` | `MediaRelay.bypass()` |
| 回传方向 | 降噪服务以 BroadcasterPeer 身份走 HTTP API 加入房间并 `produce` 回传流 | 复用 `ApiServer` + `BroadcasterPeer` |
| 防回环 | 回传流走 `broadcasterPeer.on('new-producer')` 分支，旁路入口只在 `peer.on('new-producer')`，天然隔离 | `Room.ts` |
| 生命周期 | producer close 时 `mediaRelay.stop(producer.id)`；Room close 时 `mediaRelay.close()` | `Room.ts` `handleProducerRouter` / `close()` |

### 9.3 配置

```js
// server/config.mjs
processing: {
  enabled: false,          // 主开关
  remoteIp: '127.0.0.1',   // 降噪服务监听 IP（旁路方向）
  remotePort: 5000,        // 降噪服务监听端口
},
```

> 详细设计见 `server/docs/降噪旁路项目开发文档.md`，降噪服务接入条件见
> `server/docs/降噪服务必要功能条件.md`。

项目改造路径参考 
https://mediasoup.org/documentation/v3/communication-between-client-and-server/#consuming-media-in-an-external-endpoint